= Test 1

+ a) Let $f("Mara") = "Aurelio", f("Aurelio") = "Andrea", f("Andrea") = "Mara"$.

  b) An idempotent map $e$ could map all elements to Mara.

  c) Let $B = {b}$, and let $s : B arrow.r A$ map $s(b) = "Mara"$. Let $r$ be the only map available from $A$ to $B$. Note that $r compose s$ maps $b$ to $b$, so $r compose s = 1_B$. Similarly, $s compose r$ maps all elements in $A$ to Mara.

+ The inverse map $g = (x + 7) / 4$.

  a) $(g compose f)(x) = g(4x - 7) = (4x - 7 + 7) / 4 = x$.

  b) $(f compose g)(x) = f((x+7)/4) = 4((x+7)/4)-7 = x$.
